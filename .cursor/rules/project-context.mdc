---
description: Dater backend context and active conventions
alwaysApply: true
---

# Backend Context (`dater`)

## Stack
- Spring Boot 4, Java 25, PostgreSQL, Flyway, JPA/Hibernate.
- JWT access + refresh tokens.
- Test stack: JUnit, MockMvc, Testcontainers.

## Platform baseline
- Backend runtime baseline is now Java 25 + Spring Boot 4.
- Build and verification flow is aligned with the new platform baseline.

## Domain flows
- Dates: create/update/list; list supports geo radius filtering.
- Attendees: request, accept/reject, cancel while waitlisted.
- Chat: thread-scoped between date owner and accepted attendee.
- Moderation:
  - users can `report`, `block`, and `report+block`,
  - strict block enforcement prevents profile/date/attendee/chat interactions when either side blocked the other.
- Notifications:
  - in-app notifications (`notifications` table)
  - push notifications via Expo when push token exists
  - per-type user preferences
  - deep-link metadata in push payload (`dateId`, `notificationType`)

## Reliability rules
- Core user actions (accept/reject/send message) should not fail solely because notification dispatch fails.
- Keep logging level production-friendly; avoid temporary debug noise.
- Flyway migration status should be visible during startup; keep auto-migration wiring and startup logs intact.
- Avoid per-item moderation block checks in collection flows; use bulk blocked-user lookups for date/attendee list filtering.
- Keep moderation report anti-spam guard active to reject repeated same-reason reports in a short cooldown window.

## Coordination with mobile
- Any backend contract changes must be mirrored in `dater-mobile/lib/api.ts` types and call paths.
- Keep docs cross-links updated:
  - backend details in `docs/`
  - mobile setup in sibling repo `../dater-mobile/docs/`

## Current backlog (high level)
- Revisit `My Dates` UX redesign (5-tile filter row: All/Created/Requested/Accepted/Past) before implementing.
- Final UI polish pass across screens.
